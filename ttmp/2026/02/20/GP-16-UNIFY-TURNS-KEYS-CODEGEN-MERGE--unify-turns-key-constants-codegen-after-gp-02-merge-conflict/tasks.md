# Tasks

## TODO

- [ ] Resolve `pkg/turns/keys.go` merge conflict by keeping payload/run-only manual constants
- [ ] Add missing inference value keys to `pkg/turns/spec/turns_codegen.yaml`
- [ ] Regenerate turns artifacts (`keys_gen.go`, `turns.d.ts`) via `go generate ./pkg/turns`
- [ ] Validate compile/tests for turns + inference/engine + provider helpers
- [ ] Add a short contributor note documenting schema/codegen as the single source of truth for turns value constants
