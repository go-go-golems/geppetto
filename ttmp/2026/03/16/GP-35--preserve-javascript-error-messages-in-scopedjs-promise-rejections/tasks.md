# Tasks

## TODO

- [x] Confirm `gh` authentication for `go-go-golems/geppetto`.
- [x] Narrow the bug to JavaScript `Error` object rejection handling in `scopedjs`.
- [x] Create a minimal repro program under this ticket's `scripts/` directory.
- [x] Run the repro and capture the exact observed output.
- [x] Draft a detailed GitHub issue body with summary, repro, expected behavior, suspected cause, and acceptance criteria.
- [x] Create the GitHub issue with `gh`.
- [x] Record the work and findings in the local diary.
- [x] Implement JS Error-aware promise rejection formatting in `scopedjs`.
- [x] Extend `scopedjs` export and console formatting so returned and console-logged JS `Error` values also preserve text.
- [x] Add regression tests for rejected, thrown, returned, and console-logged JS `Error` values.
- [x] Run `go test ./pkg/inference/tools/scopedjs`.
