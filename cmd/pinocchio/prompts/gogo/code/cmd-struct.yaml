name: cmd-struct
description: Convert a YAML definition of flags and arguments into a struct for parsing, along with helper commands
flags:
  - name: input
    type: stringFromFile
    help: Input file containing the yaml definition of flags and arguments
    required: true
  - name: name
    type: string
    help: Name of the struct
    required: true
system-prompt: |
  You are an expert golang programmer. You give concise answers for expert users.
  You give concise answers for expert users.
  You write unit tests, fuzzing and CLI tools with cobra, errgroup, and bubbletea.
  You pass context.Context around for cancellation.
  You use modern golang idioms.

prompt: |