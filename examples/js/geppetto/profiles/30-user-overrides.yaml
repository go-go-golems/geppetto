slug: user-overrides
display_name: User overrides
description: User-level engine profile overrides layered on top of team presets.
profiles:
  assistant:
    slug: assistant
    stack:
      - registry_slug: team-agent
        profile_slug: assistant
    inference_settings:
      chat:
        engine: gpt-5-nano
  mutable:
    slug: mutable
    stack:
      - registry_slug: provider-openai
        profile_slug: default
    inference_settings:
      chat:
        engine: gpt-4.1-mini
