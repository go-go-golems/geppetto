slug: provider-openai
display_name: Provider defaults
description: Provider baseline runtime and API settings.
profiles:
  default:
    slug: default
    display_name: Provider default
    runtime:
      system_prompt: You are the provider default assistant.
      middlewares:
        - name: retry
          id: provider-retry
          config:
            maxAttempts: 2
